function cleanPreviewEscape(value) { return String(value ?? '').replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&apos;', '"': '&quot;' }[character])); }
function cleanPreviewNumber(value, fallback) { const match = String(value || '').match(/-?[\d.]+/); return match ? Number(match[0]) : fallback; }
function cleanPreviewSource(item) { if (item.preview) return item.preview; const color = item.color || '#fff'; const text = item.text || ''; const icon = item.icon || ''; const image = item.image || ''; const textPosition = item.textPosition || {}; const iconPosition = item.iconPosition || {}; const imagePosition = item.imagePosition || {}; const textX = 500 + (cleanPreviewNumber(textPosition.left, 34) - 34) * 5; const textY = 540 + (cleanPreviewNumber(textPosition.top, 38) - 38) * 4; const iconX = 620 + (cleanPreviewNumber(iconPosition.left, 58) - 58) * 4; const iconY = 475 + (cleanPreviewNumber(iconPosition.top, 22) - 22) * 4; const imageX = 420 + (cleanPreviewNumber(imagePosition.left, 33) - 33) * 4; const imageY = 430 + (cleanPreviewNumber(imagePosition.top, 25) - 25) * 4; const imageSize = Math.max(55, Math.min(220, cleanPreviewNumber(item.imageSize, 100))); const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="1000" height="1000"><rect width="1000" height="1000" fill="#eee8dc"/><path d="M365 330C365 175 635 175 635 330" fill="none" stroke="#263a31" stroke-width="22" stroke-linecap="round"/><path d="M285 300H715V790C715 825 690 850 655 850H345C310 850 285 825 285 790Z" fill="${cleanPreviewEscape(color)}" stroke="#263a31" stroke-width="4"/><path d="M305 330H695V770C695 800 675 820 645 820H355C325 820 305 800 305 770Z" fill="#fff" opacity=".1"/>${image ? `<image href="${cleanPreviewEscape(image)}" x="${imageX}" y="${imageY}" width="${imageSize}" height="${imageSize}" preserveAspectRatio="xMidYMid meet"/>` : ''}${text ? `<text x="${textX}" y="${textY}" text-anchor="middle" fill="${cleanPreviewEscape(item.textColor || '#263a31')}" font-family="${cleanPreviewEscape(item.font || 'cursive')}" font-size="${Math.max(16, Math.min(70, cleanPreviewNumber(item.textSize, 31)))}" transform="rotate(-5 ${textX} ${textY})">${cleanPreviewEscape(text)}</text>` : ''}${icon ? `<text x="${iconX}" y="${iconY}" text-anchor="middle" fill="${cleanPreviewEscape(item.iconColor || item.textColor || '#263a31')}" font-family="cursive" font-size="${Math.max(20, Math.min(100, cleanPreviewNumber(item.iconSize, 42)))}" transform="rotate(8 ${iconX} ${iconY})">${cleanPreviewEscape(icon)}</text>` : ''}</svg>`; return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`; }
function cleanPreviewItem(order) { return (Array.isArray(order.items) ? order.items : order.design || []).find((item) => item.preview || item.image || item.text || item.icon) || {}; }
function cleanProductionCards(orders) { document.querySelectorAll('.production-card').forEach((card) => { const order = orders.find((entry) => entry.reference === card.dataset.reference); const button = card.querySelector('.production-preview-trigger'); if (!order || !button) return; const source = cleanPreviewSource(cleanPreviewItem(order)); button.innerHTML = `<div class="production-design-preview preview-snapshot"><img src="${source}" alt="Diseño de la tote personalizada" /></div>`; button.onclick = () => { const modal = document.createElement('div'); modal.className = 'production-preview-modal'; modal.innerHTML = `<div class="production-preview-dialog"><button class="modal-close" type="button" aria-label="Cerrar">×</button><p class="admin-kicker">diseño de producción</p><h2>#${order.reference}</h2><div class="production-design-preview production-preview-large preview-snapshot"><img src="${source}" alt="Diseño de la tote personalizada" /></div><div class="production-preview-actions"><a class="row-action" href="${source}" download="diseno-${order.reference}.svg">descargar diseño ↗</a></div></div>`; document.body.append(modal); modal.querySelector('.modal-close').onclick = () => modal.remove(); modal.onclick = (event) => { if (event.target === modal) modal.remove(); }; }; }); }
document.addEventListener('ivbags-admin-data-ready', (event) => setTimeout(() => cleanProductionCards(event.detail.orders || []), 100));
setTimeout(() => { if (window.ivbagsAdminOrders) cleanProductionCards(window.ivbagsAdminOrders); }, 1000);
